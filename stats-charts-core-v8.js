(()=>{function monthData(){const y=Number(yearSel.value),m=Number(monthSel.value);return rows.filter(x=>{const d=new Date(x.spent_at);return d.getFullYear()===y&&d.getMonth()+1===m})}function group(data,key){const out={};data.forEach(x=>{const k=key(x)||'未分类';out[k]=(out[k]||0)+Number(x.amount||0)});return out}function pieData(obj){const a=Object.entries(obj).sort((a,b)=>b[1]-a[1]);const total=a.reduce((s,x)=>s+x[1],0);let angle=-90;return {a,total,parts:a.map((x,i)=>{const start=angle,end=angle+(total?x[1]/total*360:0);angle=end;return [x[0],x[1],start,end,i]})}}function point(cx,cy,r,a){const q=(a-90)*Math.PI/180;return [cx+r*Math.cos(q),cy+r*Math.sin(q)]}function arc(cx,cy,r,a1,a2){const p1=point(cx,cy,r,a2),p2=point(cx,cy,r,a1),large=a2-a1<=180?0:1;return `M ${cx} ${cy} L ${p1[0]} ${p1[1]} A ${r} ${r} 0 ${large} 0 ${p2[0]} ${p2[1]} Z`}function pie(obj,title){const d=pieData(obj);if(!d.total)return `<div class="chartBox"><b>${title}</b><div class="hint">暂无数据</div></div>`;const colors=['#5f8068','#9fb7a5','#d5b98f','#8aa6b8','#c99797','#b7a3c9','#d3c478','#8fb9aa'];const paths=d.parts.map(x=>`<path d="${arc(70,70,62,x[2],x[3])}" fill="${colors[x[4]%colors.length]}"></path>`).join('');const legend=d.a.map((x,i)=>`<div class="chartLegend"><span><i style="background:${colors[i%colors.length]}"></i>${esc(x[0])}</span><b>${money(x[1])} · ${Math.round(x[1]/d.total*100)}%</b></div>`).join('');return `<div class="chartBox"><b>${title}</b><div class="pieRow"><svg viewBox="0 0 140 140">${paths}<circle cx="70" cy="70" r="34" fill="#fffdfa"></circle><text x="70" y="67" text-anchor="middle" font-size="10" fill="#7b817c">合计</text><text x="70" y="84" text-anchor="middle" font-size="13" font-weight="700">${money(d.total)}</text></svg><div>${legend}</div></div></div>`}function bars(obj,title){const a=Object.entries(obj).sort((a,b)=>b[1]-a[1]),mx=Math.max(1,...a.map(x=>x[1]));return `<div class="chartBox"><b>${title}</b>${a.length?a.map(x=>`<div class="chartBar"><div><span>${esc(x[0])}</span><b>${money(x[1])}</b></div><p><i style="width:${x[1]/mx*100}%"></i></p></div>`).join(''):'<div class="hint">暂无数据</div>'}</div>`}function ensure(){if(document.getElementById('chartGrid8'))return;const wrap=document.createElement('div');wrap.innerHTML='<div class="title">图表概览</div><div id="chartGrid8"></div>';opStats.parentNode.insertBefore(wrap,opStats.previousElementSibling);const link=document.createElement('link');link.rel='stylesheet';link.href='stats-charts-v8.css';document.head.appendChild(link)}function draw(){ensure();const d=monthData();chartGrid8.innerHTML=bars(group(d,x=>x.category),'消费分类 · 条状图')+pie(group(d,x=>x.consumer),'谁消费了多少 · 饼图')}window.drawStatsCharts8=draw;setTimeout(draw,300)})();