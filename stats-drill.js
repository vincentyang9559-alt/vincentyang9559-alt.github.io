function fmtTime(v){return new Date(v).toLocaleString('zh-CN',{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit',hour12:false})}
function showFiltered(type,key){let y=Number(yearSel.value),m=Number(monthSel.value);let r=rows.filter(x=>{let d=new Date(x.spent_at);if(d.getFullYear()!=y||d.getMonth()+1!=m)return false;if(type==='operator')return x.operator===key;if(type==='consumer')return x.consumer===key;if(type==='category')return x.category===key;if(type==='nature')return (x.nature||'必要')===key;return true});detail.innerHTML=r.map(x=>'<div class="row"><div><div class="meta">'+fmtTime(x.spent_at)+'</div><b>'+esc(x.category)+(x.note?' · '+esc(x.note):'')+'</b><div><span class="tag">消费者：'+esc(x.consumer)+'</span><span class="tag blue">'+esc(x.operator)+' · '+esc(x.source)+'</span></div></div><div class="money">'+money(x.amount)+'</div></div>').join('')||'<div class="hint">没有明细</div>';detail.scrollIntoView({behavior:'smooth'})}